<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <style>
        .posts {display: flex; gap: 24px; align-items: flex-start;}
        .postitem {flex: 1; min-width: 320px;}
        .card {border: 4px solid #6b00d6; padding: 10px; margin: 10px 0px;}
    </style>
</head>
<body>
    <h1>Добро пожаловать, <%= user.username %> (id: <%= user.id %>)!</h1>

    <div class="posts">
        <div class="postitem">
            <h2>Последние посты</h2>
            <% (postsAll || []).forEach(function(post){ %>
                <%- include('parts/postCard', {post: post, user: user}) %>
            <% }); %>
        </div>

        <div class="postitem">
            <h2>Посты из подписок</h2>
            <% if ((postsSubs || []).length === 0) { %>
                <p>Пока нет постов от авторов, на которых вы подписаны.</p>
            <% } %>
            <% (postsSubs || []).forEach(function(post){ %>
                <%- include('parts/postCard', {post: post, user: user}) %>
            <% }); %>
        </div>
    </div>

    <% if (user.role === 'author') { %>
        <a href="/author/createPost">
            <button type="button">Создать пост</button>
        </a>
    <% } %>

    <% if (user.role === 'admin') { %>
        <a href="/admin/deleteUser">
            <button type="button">Удалить пользователя</button>
        </a>
    <% } %>
</body>
</html>